import "./src/scss/main.scss";
import { initializeDashboard } from "./src/scripts";

// App entry
document.addEventListener("DOMContentLoaded", () => {
  initializeDashboard(); // Setup the initial UI
});
